{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://game-hall.local/schemas/game-plugin-v1.json",
  "title": "Game Hall third-party game plugin",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "apiVersion",
    "enabled",
    "id",
    "name",
    "description",
    "category",
    "tone",
    "players"
  ],
  "properties": {
    "$schema": { "type": "string" },
    "apiVersion": { "const": 1 },
    "enabled": { "type": "boolean" },
    "id": {
      "type": "string",
      "maxLength": 32,
      "pattern": "^plugin-[a-z0-9][a-z0-9-]{0,24}$"
    },
    "name": { "type": "string", "minLength": 1, "maxLength": 24 },
    "description": { "type": "string", "minLength": 1, "maxLength": 80 },
    "category": { "type": "string", "minLength": 1, "maxLength": 16 },
    "tone": {
      "type": "string",
      "minLength": 1,
      "maxLength": 24,
      "pattern": "^[a-z0-9][a-z0-9-]{0,23}$"
    },
    "players": {
      "type": "object",
      "additionalProperties": false,
      "required": ["min", "max"],
      "properties": {
        "min": { "type": "integer", "minimum": 1, "maximum": 20 },
        "max": { "type": "integer", "minimum": 1, "maximum": 20 },
        "label": { "type": "string", "minLength": 1, "maxLength": 16 }
      }
    },
    "defaultOptions": { "type": "object" },
    "ruleLabels": {
      "type": "array",
      "maxItems": 6,
      "items": { "type": "string", "minLength": 1, "maxLength": 24 }
    }
  }
}
