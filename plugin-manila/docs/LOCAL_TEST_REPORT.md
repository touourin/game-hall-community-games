# 马尼拉本地测试报告

验收日期：2026-09-03  
规则版本：Zoch 2005 基础常规规则（不启用满船逐客增强变体）

## 自动化结果

| 检查 | 结果 |
| --- | --- |
| 建模数据验证 | 通过：20 张份额、4 种货物、14 个场景、3 个 schema 示例、2 张 SVG 建模图、1 份 PDF 规则书 |
| 马尼拉后端规则与真实浏览器夹具 | `47 passed` |
| 马尼拉 Vue 组件 | `14 passed` |
| 社区插件回归（排除下述无关目录） | `302 passed` |
| 大厅前端回归 | `106` 个测试文件、`617 passed` |
| TypeScript | 插件独立 `vue-tsc` 与大厅 `vue-tsc -b` 均通过 |
| 生产构建 | Vite 通过，转换 `2472` 个模块，马尼拉视图与两套目录图均进入产物 |
| 注册与主题 | 后端 14 个发布游戏校验通过；目录生成、图标、主题选择器与主题对比度检查通过 |

## 真实浏览器验收

浏览器使用真实 `ManilaEngine` 快照和 FastAPI 测试夹具，而不是手写静态 JSON。

| 项目 | 结果 |
| --- | --- |
| 3 人完整牌局 | 63 个动作、4 次航行，正常到达终局并生成共同胜者 |
| 4 人完整牌局 | 107 个动作、6 次航行，正常到达终局并生成唯一胜者 |
| 5 人完整牌局 | 99 个动作、5 次航行，正常到达终局并生成共同胜者 |
| 阶段场景 | auction、placement、movement、pirate、pilot、settlement、finished 全部可渲染，状态文案与合法动作相符 |
| 动画 | dice_roll、punt_move、worker_move、pirate_board、pirate_plunder、pilot_move、share_deal、mortgage、settlement 全部触发；1.25 秒后清理 |
| 动画边界 | 动画层与牌桌边界一致，`overflow: hidden`，九类动画不改变文档尺寸且中段模型仍与牌桌相交 |
| 模型碰撞 | placement、pirate、settlement、finished 场景中没有模型越出牌桌；三艘货船无相互重叠 |
| 1440×900 | 三列主场景与底部私密区同屏；无页面横向溢出 |
| 900×900 | 市场单行、目的地区双列；页面无横向溢出 |
| 390×844 | 页面无横向溢出；玩家、市场、航线、特殊位和操作内容仅在局部容器滚动 |
| 规则面板 | 可开关；严格拍卖、恰停 13、保险 +10、终局公式及基础变体边界均可见 |
| 控制台 | 复测结束时 0 个 error/warn |
| 建模一致性 | 与 `table-scene-blueprint.svg` 的顶部玩家轨、左市场、中航线、右目的地、底部私密区一致；四种卡面代码、颜色和原创纹样与 `share-card-sheet.svg` 一致 |

浏览器巡检中曾发现响应式文件加载顺序覆盖模型断点规则。现已把断点拆到最后加载的 `responsive.css`；复测后，手机市场高度由 405px 收敛到 180px，航线内部画布恢复为 680px 局部横滑，页面仍无横向溢出。

## 已知的仓库外部阻塞

大厅的统一 `plugins:verify-boundaries` 和不带过滤的社区 Python 聚合命令目前会被同一工作区中、非本实现的未跟踪目录 `plugin-dead-mans-draw` 阻塞：其 `frontend/vitest.config.ts` 越界导入且测试包缺少可解析的 `tests.helpers`。未修改该目录。排除该目录后社区回归为 `302 passed`；马尼拉独立测试、全前端测试、类型检查、生产构建及注册校验均通过。
